// Declaração das variáveis
let INDICE = 12;
let SOMA = 0;
let K = 1;

// Loop para incrementar SOMA e K
while (K < INDICE) {
    K = K + 1;
    SOMA = SOMA + K;
}

// Exibir o valor final de SOMA
console.log("O valor final de SOMA é:", SOMA);
